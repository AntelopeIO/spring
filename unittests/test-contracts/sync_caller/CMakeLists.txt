if( EOSIO_COMPILE_TEST_CONTRACTS )
   add_contract( sync_caller sync_caller sync_caller.cpp )
else()
   configure_file( ${CMAKE_CURRENT_SOURCE_DIR}/sync_caller.wasm ${CMAKE_CURRENT_BINARY_DIR}/sync_caller.wasm COPYONLY )
   configure_file( ${CMAKE_CURRENT_SOURCE_DIR}/sync_caller.abi  ${CMAKE_CURRENT_BINARY_DIR}/sync_caller.abi COPYONLY )
endif()
